#include<stdio.h>
int main()
{
 float radius,area;
 printf("enter the radius value");
 scanf("%f",&radius);
 area=3.14*radius*radius;
 printf("Area of circle %f is having the radius %f",area,radius);
 return 0;
}

